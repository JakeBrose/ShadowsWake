extends index.pug

block content

  div
    ul.profile
      li Player Profile
      li= `Name: ${userData.first_name} ${userData.last_name}`
      li= `Username: ${userData.username}`
      li= `Email: ${userData.email}`
      li= `Wins: ${userData.wins}`
      li= `Losses: ${userData.losses}`
    ul.profile
      li
        a(href="/newGame") Begin your adventure!